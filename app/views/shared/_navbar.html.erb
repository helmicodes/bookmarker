<header aria-label="Site Header" class="bg-white shadow-sm">
  <div class="mx-auto flex h-16 max-w-screen-xl items-center gap-8 px-4 sm:px-6 lg:px-8">
    <div class="flex flex-1 items-center justify-end md:justify-between">
      <nav aria-label="Site Nav" class="hidden md:block">
        <ul class="flex items-center gap-6 text-sm">
          <li>
            <a class="text-gray-500 transition hover:text-gray-500/75" href="/">
            </a>
          </li>
        </ul>
      </nav>

      <div class="flex items-center gap-4">
        <% if signed_in? %>
          <div class="sm:flex sm:gap-4">
            <div class="self-center"><%= current_user.username %></div>
            <%= button_to "Logout", session_path, method: :delete, class: "rounded-md bg-gray-100 px-5 py-2.5 text-sm font-medium text-blue-500 sm:block" %>
          </div>
        <% else %>
          <div class="sm:flex sm:gap-4">

            <a
              class="block rounded-md bg-blue-500 px-5 py-2.5 text-sm font-medium text-white transition"
              href="/signin"
            >
              Login
            </a>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</header>
