<form action="/links" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <label>Paste a URL here to save it for later:</label>
  <div class="flex justify-between gap-2 mt-2">
    <input type="text" placeholder="https://..." name="link[url]" id="link_url"
           class="w-full px-3 py-2 leading-tight text-gray-700 border rounded shadow appearance-none focus:outline-none focus:shadow-outline">
    <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded-lg inline-flex items-center">
      Save
    </button>
  </div>
</form>